void BubbleSort(int A[], int N){
    int i, j, l=0, r=N-1;
    int temp;
    for (i = l; i < r; i++) {
        for (j = l; j < r – i +l; j++)
             if (A[j] > A[j+1]) {
                 temp = A[j];
                 A[j] = A[j+1];
                 A[j+1] = temp;
             }
      }
      return;
}